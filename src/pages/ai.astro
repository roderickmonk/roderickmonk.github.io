---
import Layout from "../layouts/Layout.astro";
---

<Layout title="AI">

  <Fragment slot="head">
    <script is:inline>
      function initNeuralNet() {
        new p5(function(p) {
          var W = 440, H = 286;
          var layers = [4, 5, 5, 3];
          var nodes = [];
          var conns = [];
          var tick = 0;
          var labels = ['Input', 'Hidden', 'Hidden', 'Output'];

          p.setup = function() {
            var cnv = p.createCanvas(W, H);
            cnv.parent('nn-canvas');
            cnv.style('max-width', '100%');
            cnv.style('height', 'auto');

            for (var l = 0; l < layers.length; l++) {
              var col = [];
              var x = p.map(l, 0, layers.length - 1, 70, W - 70);
              for (var n = 0; n < layers[l]; n++) {
                var y = p.map(n, 0, layers[l] - 1, H * 0.15, H * 0.78);
                col.push({ x: x, y: y });
              }
              nodes.push(col);
            }

            for (var l = 0; l < layers.length - 1; l++) {
              for (var i = 0; i < nodes[l].length; i++) {
                for (var j = 0; j < nodes[l + 1].length; j++) {
                  conns.push({
                    x0: nodes[l][i].x, y0: nodes[l][i].y,
                    x1: nodes[l + 1][j].x, y1: nodes[l + 1][j].y,
                    phase: Math.random()
                  });
                }
              }
            }
          };

          p.draw = function() {
            p.background(15, 23, 42);
            tick += 0.012;

            // Connections + travelling pulses
            for (var ci = 0; ci < conns.length; ci++) {
              var c = conns[ci];
              p.stroke(56, 189, 248, 35);
              p.strokeWeight(0.7);
              p.line(c.x0, c.y0, c.x1, c.y1);

              var t = (tick * 0.55 + c.phase) % 1;
              var px = p.lerp(c.x0, c.x1, t);
              var py = p.lerp(c.y0, c.y1, t);
              var fade = Math.sin(t * Math.PI);
              p.noStroke();
              p.fill(168, 85, 247, fade * 220);
              p.ellipse(px, py, 4, 4);
            }

            // Nodes
            for (var l = 0; l < nodes.length; l++) {
              for (var n = 0; n < nodes[l].length; n++) {
                var nd = nodes[l][n];
                var act = 0.5 + 0.5 * Math.sin(tick * 1.3 + l * 1.1 + n * 0.7);
                p.noStroke();
                p.fill(56, 189, 248, 25 + act * 30);
                p.ellipse(nd.x, nd.y, 20 + act * 8, 20 + act * 8);
                p.fill(
                  Math.round(30  + act * 138),
                  Math.round(64  + act * 21),
                  Math.round(175 + act * 72)
                );
                p.ellipse(nd.x, nd.y, 13, 13);
              }
            }

            // Layer labels
            p.noStroke();
            p.fill(148, 163, 184);
            p.textSize(9);
            p.textAlign(p.CENTER);
            for (var l = 0; l < nodes.length; l++) {
              p.text(labels[l], nodes[l][0].x, H - 6);
            }
          };
        });
      }
    </script>
    <script defer src="https://cdn.jsdelivr.net/npm/p5@1.11.2/lib/p5.min.js" onload="initNeuralNet()"></script>
  </Fragment>

  <div class="space-y-6">

    <!-- Page header -->
    <div class="rounded-2xl border border-slate-200 bg-white p-8 shadow-sm">
      <h1 class="text-3xl font-bold mb-2">Artificial Intelligence</h1>
      <p class="text-slate-500 text-sm">Understanding AI — and preparing students to thrive alongside it.</p>
    </div>

    <!-- Background + neural-net graphic -->
    <div class="rounded-2xl border border-slate-200 border-t-4 border-t-blue-600 bg-white p-8 shadow-sm">
      <div class="grid gap-8 md:grid-cols-2">

        <div>
          <h2 class="text-xl font-semibold mb-3">My AI Background</h2>
          <p class="text-slate-700 text-sm leading-relaxed mb-4">
            I completed the <strong>Artificial Intelligence Professional Program at Stanford University</strong>,
            covering machine learning, deep learning, reinforcement learning, and generative AI from
            mathematical first principles. That formal study builds on 25 years of industry
            experience — a decade as CTO of an enterprise software company, and more than a decade
            running an independent engineering practice where AI and data-intensive systems were
            central to the work.
          </p>
          <p class="text-slate-700 text-sm leading-relaxed">
            I understand how these systems actually work — not just how to use them. My goal is to bring
            that depth into the classroom in a way that is accessible, honest, and genuinely useful
            for every student, regardless of where they end up.
          </p>
        </div>

        <div>
          <div id="nn-canvas" class="rounded-lg overflow-hidden border border-slate-200"></div>
          <p class="mt-2 text-xs text-slate-400 text-center">A simulated neural network — the architecture behind modern AI systems.</p>
        </div>

      </div>
    </div>

    <!-- What is AI — three columns -->
    <div class="rounded-2xl border border-slate-200 border-t-4 border-t-blue-600 bg-white p-8 shadow-sm">
      <h2 class="text-xl font-semibold mb-6">What Is AI?</h2>
      <div class="grid gap-5 md:grid-cols-3">

        <div class="rounded-xl overflow-hidden bg-slate-50">
          <div class="h-1.5 bg-blue-600"></div>
          <div class="p-5">
            <h3 class="font-semibold text-slate-900 mb-2">Machine Learning</h3>
            <p class="text-sm text-slate-600 leading-relaxed">
              Rather than following hand-written rules, ML systems learn patterns directly from data.
              A spam filter, a music recommendation, a medical diagnosis tool — all powered by
              learning from millions of examples rather than explicit programming.
            </p>
          </div>
        </div>

        <div class="rounded-xl overflow-hidden bg-slate-50">
          <div class="h-1.5 bg-violet-500"></div>
          <div class="p-5">
            <h3 class="font-semibold text-slate-900 mb-2">Generative AI</h3>
            <p class="text-sm text-slate-600 leading-relaxed">
              Tools like ChatGPT and Claude are built on large language models trained on vast
              amounts of text. They generate fluent, plausible responses — but they do not reason
              or understand the way humans do. Knowing the difference matters.
            </p>
          </div>
        </div>

        <div class="rounded-xl overflow-hidden bg-slate-50">
          <div class="h-1.5 bg-emerald-500"></div>
          <div class="p-5">
            <h3 class="font-semibold text-slate-900 mb-2">Responsible Use</h3>
            <p class="text-sm text-slate-600 leading-relaxed">
              AI amplifies both capability and risk. Students learn to evaluate AI outputs
              critically, recognize bias and error, respect academic integrity, and use these
              tools as thinking aids — not replacements for their own judgment.
            </p>
          </div>
        </div>

      </div>
    </div>

    <!-- AI in the classroom -->
    <div class="rounded-2xl border border-slate-200 border-t-4 border-t-blue-600 bg-white p-8 shadow-sm">
      <h2 class="text-xl font-semibold mb-3">AI Across the Curriculum</h2>
      <p class="text-sm text-slate-600 leading-relaxed mb-6">
        AI literacy is not a separate subject — it grows naturally out of mathematics and computer
        studies. The statistics and probability taught in senior math underpin machine learning.
        The Python programming in CS courses is the same language used by AI practitioners worldwide.
        And the logical reasoning developed throughout provides the critical lens for evaluating
        what AI systems actually do.
      </p>
      <div class="grid gap-4 md:grid-cols-2">

        <div class="flex gap-3 items-start">
          <div class="mt-1.5 w-1.5 h-1.5 rounded-full bg-blue-600 shrink-0"></div>
          <div>
            <p class="font-medium text-sm text-slate-900">Mathematical foundations</p>
            <p class="text-xs text-slate-500 mt-0.5">Statistics, probability, and linear thinking are the language of machine learning</p>
          </div>
        </div>

        <div class="flex gap-3 items-start">
          <div class="mt-1.5 w-1.5 h-1.5 rounded-full bg-blue-600 shrink-0"></div>
          <div>
            <p class="font-medium text-sm text-slate-900">Hands-on experimentation</p>
            <p class="text-xs text-slate-500 mt-0.5">Students interact with real AI systems and learn to interpret — and question — their outputs</p>
          </div>
        </div>

        <div class="flex gap-3 items-start">
          <div class="mt-1.5 w-1.5 h-1.5 rounded-full bg-blue-600 shrink-0"></div>
          <div>
            <p class="font-medium text-sm text-slate-900">Ethical reasoning</p>
            <p class="text-xs text-slate-500 mt-0.5">Privacy, bias, and accountability are real issues that require thoughtful, informed citizens</p>
          </div>
        </div>

        <div class="flex gap-3 items-start">
          <div class="mt-1.5 w-1.5 h-1.5 rounded-full bg-blue-600 shrink-0"></div>
          <div>
            <p class="font-medium text-sm text-slate-900">Career awareness</p>
            <p class="text-xs text-slate-500 mt-0.5">AI is reshaping every profession — students deserve to understand what is coming and why it matters</p>
          </div>
        </div>

      </div>
    </div>

  </div>

</Layout>
